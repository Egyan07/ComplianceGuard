# FastAPI and ASGI server
# fastapi>=0.110 is the first release with Python 3.13 wheels upstream.
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1

# Database and ORM
# sqlalchemy>=2.0.30 ships wheels that work with Python 3.13's typing module.
sqlalchemy>=2.0.32,<3
alembic>=1.13,<2
psycopg2-binary>=2.9.9

# Authentication and security
PyJWT>=2.8,<3
bcrypt>=4.1,<5
python-multipart>=0.0.9

# Environment and configuration
# pydantic>=2.10 has prebuilt pydantic-core wheels for Python 3.13, so
# `pip install` no longer requires a Rust toolchain on the install host.
pydantic>=2.10,<3
pydantic-settings>=2.6,<3
python-dotenv>=1.0

# HTTP and API clients
httpx>=0.26,<1
requests>=2.31
aiosmtplib>=3.0.0

# Rate limiting
slowapi==0.1.9

# Error monitoring
sentry-sdk[fastapi]>=2.0.0

# Utilities
PyYAML>=6.0,<7
email-validator==2.1.0
python-dateutil==2.8.2
pytz==2023.3

# Development and testing (can be moved to dev-requirements.txt)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
factory-boy==3.3.0

# Code quality
black==23.11.0
isort==5.12.0
flake8==6.1.0

# AWS Integration
boto3==1.34.0
botocore==1.34.0

# License key verification
cryptography>=42.0.0
