# Dependencies for CI test runs (backend-tests job) and local test runs.
# Mirrors the ranges in requirements.txt (so tests exercise the same
# dependency surface as production) plus the pytest toolchain.
# Code quality is handled by ruff (see pyproject.toml), not black/isort/flake8.
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
sqlalchemy>=2.0.32,<3
alembic>=1.13,<2
psycopg2-binary>=2.9.9
PyJWT>=2.8,<3
bcrypt>=4.1,<5
python-multipart>=0.0.9
pydantic>=2.10,<3
pydantic-settings>=2.6,<3
python-dotenv>=1.0
slowapi==0.1.10
email-validator>=2.3.0
python-dateutil>=2.9.0.post0
pytz>=2024.1
httpx>=0.26,<1
boto3>=1.43.81
botocore>=1.43.81
sentry-sdk[fastapi]>=2.0.0
aiosmtplib>=3.0.0
cryptography>=42.0.0
PyYAML>=6.0,<7
prometheus-client>=0.20,<1

# Test tooling
pytest>=7.4
pytest-asyncio>=0.21
pytest-cov>=4.1
