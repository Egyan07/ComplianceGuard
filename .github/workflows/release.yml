# This workflow is intentionally disabled.
# All releases are handled by the 'release' job in ci.yml.
# Keeping this file to prevent accidental recreation.
name: Release (disabled)

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is disabled. Use ci.yml release job.'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Releases are handled by ci.yml. This workflow is a placeholder."
